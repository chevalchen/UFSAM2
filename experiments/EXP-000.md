# EXP-000 — SANSA Reference Baseline 与 Local Control 约定

> **Experiment ID：** `EXP-000`
> **状态：** `DECIDED`
> **最终裁决：** `ACCEPTED_BY_OWNER_ASSUMPTION`
> **证据标签：** 论文表格为 `LITERATURE`；重启前复现判断为 `HISTORICAL`；各候选实验中同 run 生成的 matched `B0` 才能作为该实验的 causal control。
> **权威性：** 本文件是 EXP-000 的唯一人类可审阅权威记录。
> **全局依据：** `UNCERTAINTY_SAM2_FSS_RESEARCH_SPEC.md`
> **全局索引：** `EXPERIMENT_LEDGER.md`

## 1. 摘要

- Baseline policy：`SANSA_PAPER_REFERENCE_ACCEPTED`
- Local control policy：`DEFAULT-OFF_EQUIVALENCE_ASSUMED_BY_OWNER`
- 研究负责人于 2026-07-22 决定接受 SANSA paper/official tables 作为 absolute baseline，不要求重新执行完整论文 baseline 表。
- 不单独验证 MTP、BRM、support aggregation、hflip 或 AV-PMC 关闭时的 baseline equivalence。
- 正式因果比较仍必须在候选实验自身的相同 SHA、checkpoint 和 episodes 上保留第一次 baseline decode `M0/B0`；论文 aggregate number 不能当作 paired sample。
- Result run IDs：`N/A — no standalone EXP-000 run`

## 2. 假设与协议决定

研究负责人接受论文表格作为 COCO-20i、FSS-1000、PASCAL-Part、PACO-Part generalist 与 strict-FSS 的 absolute reference，并假设所有历史候选模块及 AV-PMC 在关闭时与 baseline 一致。full-paper baseline reproduction 与 standalone default-off equivalence check 均免除。

若后续候选实验的 matched `B0` 与论文表格出现重大冲突，必须回到 Idea/Decision 阶段审查本假设，不能静默重解释结果。

## 3. 冻结合同

### 3.1 权威源码与版本

- SANSA upstream commit：`f562b5642940e08a57e3da2608b804b99af3763b`
- 本地 nested repository：`SANSA/`
- Upstream remote：`https://github.com/chevalchen/SANSA.git`
- UF-SAM 顶层 authority import commit：`000d450`
- 导入前核对：donor working tree 的 `101/101` 个 tracked files 与 Git index blob 完全一致。
- 论文来源：`Paper/SANSA1.pdf`，arXiv `2505.21795v2`（2025-11-15）；`Paper/SANSA2.pdf` 为 appendix；`SANSA/README.md` 提供 official commands/tables。

### 3.2 已批准的本地修复

- `inference_fss.py` 与 `util/visualization.py` 的改动属于 visualization-only。
- `util/commons.py::resize_mask` 的阈值从 `>0` 改为 `>0.5`，会影响 mask-only benchmark 的 support-mask resize，因此属于研究负责人批准的 shared mask-prompt preprocessing fix，而非任何候选模块变量。
- 修复必须保持独立 commit，并在候选实验的所有 causal rows、datasets/folds 与训练/评估路径中一致。

### 3.3 Checkpoint、数据与环境

- SANSA paper-released COCO-20i fold-0 adapter checkpoint SHA256：`b02b96f30ee558c37ef4fc5f889e90772dafb117e42b3fcf0945000220bc57e5`
- SAM2-Large base weights SHA256：`7442e4e9b732a508f80e141e7c2913437a3610ee0c77381a66658c3a445df87b`
- Formal checkpoint：随 dataset/fold 在对应候选实验 `FROZEN` 前确认并记录 official hash。
- Tracked config、episode manifest、folds、shots、seeds 与 environment fingerprint：不为 EXP-000 单独生成；由每个候选实验独立冻结。
- 数据协议：沿用 SANSA paper protocol；无需用 checkpoint 重建整张论文表。

### 3.4 指标与护栏

- 论文 aggregate mIoU/FB-IoU 只用于 absolute table comparison。
- 候选方法的因果效应必须使用同 episode 的 treatment-minus-`B0` paired delta 与 CI。
- `B0/M0` 必须关闭全部候选功能，绑定 clean Git SHA，写入不可覆盖的 run directory，并保留 per-episode baseline 与 treatment 输出。
- 不设置或执行独立 default-off output-equivalence tolerance。

## 4. 结果与裁决

- 未执行 standalone EXP-000 run。
- 最终裁决：`ACCEPTED_BY_OWNER_ASSUMPTION`。
- 该裁决只接受 absolute reference 与 local-control 假设，不把论文表格提升为候选实验的 paired causal evidence。

## 5. 决策历史

| 日期 | 状态变化 | 决定与依据 | Git SHA / Run ID |
|---|---|---|---|
| 2026-07-22 | `DRAFT → DECIDED` | 研究负责人免除 full-paper baseline reproduction 与 standalone default-off equivalence；候选实验仍须保存 matched `B0`。 | Authority import `000d450`; standalone run `N/A` |
